name: Inferflow CI

on:
  pull_request:
    branches: [main, develop]
    paths: ['inferflow/**']
  push:
    branches: [main, develop]
    paths: ['inferflow/**']

jobs:
  build-inferflow:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./inferflow
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'
          cache: false

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-inferflow-${{ hashFiles('inferflow/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-inferflow-

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test -v ./...

      - name: Build application
        run: go build -v ./...

      - name: Run go vet
        run: go vet ./...

      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: Run staticcheck
        run: staticcheck ./...

