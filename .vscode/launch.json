{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/horizon/cmd/horizon/main.go",
            "envFile": "${workspaceFolder}/horizon/cmd/horizon/example.env"
        }
    ]
}
