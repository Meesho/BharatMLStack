apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Values.applicationName }}-test-connection"
  labels:
{{ include "labels.common" . | indent 4 }}
{{ include "labels.chart" . | indent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Values.name }}:{{ .Values.service.port }}']
  restartPolicy: Never
